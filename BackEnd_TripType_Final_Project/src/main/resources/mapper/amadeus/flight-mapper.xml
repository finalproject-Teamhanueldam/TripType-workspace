<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="flight">

    <!-- ===============================
         FLIGHT 세그먼트 batch insert
         - PK(FLIGHT_ID)는 트리거에서 자동 생성
         - INSERT ALL 사용
         ✅ 수정 포인트
           1) Date/LocalDateTime 모두 안전하게 처리하도록 jdbcType 지정
           2) airlineId가 null일 때 ORA-01400/ORA-02291 방지 목적: jdbcType 지정
              (실제 NOT NULL/FK 정책에 따라 null 허용이면 그대로 들어가고,
               null 불가면 서비스/데이터 쪽에서 반드시 채워야 함)
         =============================== -->
    <insert id="insertFlights" parameterType="list">

        INSERT ALL
        <foreach collection="list" item="f">
            INTO TB_FLIGHT (
                FLIGHT_SEGMENT_NO,
                FLIGHT_NUMBER,
                FLIGHT_DEPART_DATE,
                FLIGHT_ARRIVE_DATE,
                FLIGHT_DURATION,
                FLIGHT_DIRECTION,
                FLIGHT_IS_DEL,
                DEPART_AIRPORT,
                DEST_AIRPORT,
                OPER_AIRLINE_ID,
                SELLING_AIRLINE_ID,
                FLIGHT_OFFER_ID
            ) VALUES (
                #{f.flightSegmentNo, jdbcType=INTEGER},
                #{f.flightNumber, jdbcType=VARCHAR},
                #{f.flightDepartDate, jdbcType=TIMESTAMP},
                #{f.flightArriveDate, jdbcType=TIMESTAMP},
                #{f.flightDuration, jdbcType=VARCHAR},
                #{f.flightDirection, jdbcType=VARCHAR},
                'N',
                #{f.departAirport, jdbcType=VARCHAR},
                #{f.destAirport, jdbcType=VARCHAR},
                #{f.operAirlineId, jdbcType=INTEGER},
                #{f.sellingAirlineId, jdbcType=INTEGER},
                #{f.flightOfferId, jdbcType=INTEGER}
            )
        </foreach>
        SELECT 1 FROM DUAL

    </insert>

</mapper>
